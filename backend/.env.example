# Backend-only environment variables.
# Do not put VITE_* variables in this file.

BACKEND_PORT=3001
TZ=Europe/Prague
# Comma-separated list of allowed frontend origins in local dev.
CLIENT_ORIGIN=http://localhost:5173

# Optional shared API header key. Leave empty to disable X-API-KEY checks.
API_KEY=

# Required auth secret for signing access tokens.
# Use a unique random value with at least 32 characters.
AUTH_TOKEN_SECRET=replace-with-32-plus-char-random-secret

ACCESS_TOKEN_TTL_MINUTES=1440
REFRESH_TOKEN_TTL_HOURS=720
REFRESH_COOKIE_NAME=worktrack_refresh_token
# Recommended for production cross-site auth. Local dev can omit these and use the built-in defaults.
REFRESH_COOKIE_SAME_SITE=None
REFRESH_COOKIE_SECURE=true
AUTH_LOGIN_WINDOW_MINUTES=15
AUTH_LOGIN_LOCK_MINUTES=15
AUTH_LOGIN_LOCK_AFTER_ATTEMPTS=5
AUTH_REGISTER_WINDOW_MINUTES=30
AUTH_REGISTER_LOCK_MINUTES=30
AUTH_REGISTER_LOCK_AFTER_ATTEMPTS=4

# Runtime database connection.
DATABASE_URL=postgresql://user:password@host:5432/database?schema=public

# Optional direct connection for Prisma CLI migrations/introspection and
# runtime fallback when the main DATABASE_URL points to a proxy.
DIRECT_DATABASE_URL=postgresql://user:password@host:5432/database?schema=public

# Local development/testing only. Never use file mode in production.
# DB_MODE=file
# DATA_FILE=./data/db.local.json
